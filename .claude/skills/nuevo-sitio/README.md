Esta carpeta es la entrevista de H6. Se invoca con `/nuevo-sitio` desde este repositorio.

El fichero que importa es `SKILL.md`: lo que dice ahí es lo que se hace. Si cambias el
esquema del blueprint o añades un módulo, actualízalo — una entrevista que pregunta por algo
que ya no existe es peor que ninguna.
